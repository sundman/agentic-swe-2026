# Image Generation CLI

This is an empty workspace for the optional image generation exercise.

## Usage

Use the prompt file at `docs/exercises/prompts/gen-image-cli-prompt.md` as the specification for building an image generation CLI tool in this directory.

## Prerequisites

You'll need API keys for at least one provider. See `.env.example` in the project root.
